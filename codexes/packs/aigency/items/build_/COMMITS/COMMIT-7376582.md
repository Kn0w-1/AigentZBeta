# Commit Brief: `7376582` — PR-4: wire KNYT Runtime to canonical NBE plan + orchestration audit trail

| Field | Value |
|-------|-------|
| SHA | [`7376582`](https://github.com/Kn0w-1/AigentZBeta/commit/7376582934c2c44f1dc9c479b49549e034d591ad) |
| Author | Claude |
| Date | 2026-05-22T01:05:16Z |
| Branch | dev (direct push) |
| Type | `feat` |
| Repo | Kn0w-1/AigentZBeta |

## Commit Message

```
PR-4: wire KNYT Runtime to canonical NBE plan + orchestration audit trail

- new useNbePlan hook reads /api/runtime/nbe (NBEPlan SoT row with id, expires_at)
- new emitClientOrchestrationEvent helper POSTs to /api/runtime/orchestration
- KnytRuntimeTab now:
  - renders disposition card from canonical NBEPlan (apiState.nbe fallback while loading)
  - emits cartridge_lead_active event on NBE launch (reason: nbe_launched, metadata.nbe_id)
  - emits specialist_invoked event on signal action (reason: signal_emitted:<action>)
  - refetches NBE after signal action so depth advancement reflects immediately
  - launches L3 codex via buildCodexUrl so personaId propagates per inter-cartridge rule

journey_stage left null on emitted events — KNYT axis labels do not map to the
canonical JourneyStage union; the original axis value is captured in metadata
under knyt_axis_stage so server-side joins can recover it without polluting the
top-level column with non-canonical strings.

https://claude.ai/code/session_01WpEKdSdKfopL9QdLAKiEym
```

## Body

- new useNbePlan hook reads /api/runtime/nbe (NBEPlan SoT row with id, expires_at)
- new emitClientOrchestrationEvent helper POSTs to /api/runtime/orchestration
- KnytRuntimeTab now:
  - renders disposition card from canonical NBEPlan (apiState.nbe fallback while loading)
  - emits cartridge_lead_active event on NBE launch (reason: nbe_launched, metadata.nbe_id)
  - emits specialist_invoked event on signal action (reason: signal_emitted:<action>)
  - refetches NBE after signal action so depth advancement reflects immediately
  - launches L3 codex via buildCodexUrl so personaId propagates per inter-cartridge rule

journey_stage left null on emitted events — KNYT axis labels do not map to the
canonical JourneyStage union; the original axis value is captured in metadata
under knyt_axis_stage so server-side joins can recover it without polluting the
top-level column with non-canonical strings.

https://claude.ai/code/session_01WpEKdSdKfopL9QdLAKiEym

## Files Changed

| Change | File |
|--------|------|
| Added | `app/hooks/useNbePlan.ts` |
| Modified | `app/triad/components/codex/tabs/KnytRuntimeTab.tsx` |
| Added | `services/orchestration/emitClientEvent.ts` |

## Stats

 3 files changed, 221 insertions(+), 24 deletions(-)
