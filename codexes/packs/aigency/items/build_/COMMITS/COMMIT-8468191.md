# Commit Brief: `8468191` — codex cache bump v10/v8/v8 — flush pre-shift snapshot after metaKnyts data realignment

| Field | Value |
|-------|-------|
| SHA | [`8468191`](https://github.com/Kn0w-1/AigentZBeta/commit/8468191f7af63536e90004ae533f6e8b0cdc9016) |
| Author | Claude |
| Date | 2026-05-16T06:02:08Z |
| Branch | dev (direct push) |
| Type | `chore` |
| Repo | Kn0w-1/AigentZBeta |

## Commit Message

```
codex cache bump v10/v8/v8 — flush pre-shift snapshot after metaKnyts data realignment

A SQL transaction shifted codex_episodes title rows from episode_number 1..13
to 0..12 (with issue_number recomputed as #0..#12) and shifted the 13 active
Keta KNYT cover rows in codex_media_assets the same way, while archiving the
two legacy "Legend of Kn0w1" motorcycle covers that were sitting at ep 0 from
the old GN-at-0 convention. Both tables now agree with master_content_qubes
on the canonical convention (episode_number IS the display number; GN at -1).

Clients holding the previous v9/v7/v7 snapshot would keep rendering the stale
duplicate motorcycle slot 2 and the +1 title offset until they hard-refresh.
Bumping the cache key forces them to drop the old snapshot and refetch
against the now-correct DB on next load.
```

## Body

A SQL transaction shifted codex_episodes title rows from episode_number 1..13
to 0..12 (with issue_number recomputed as #0..#12) and shifted the 13 active
Keta KNYT cover rows in codex_media_assets the same way, while archiving the
two legacy "Legend of Kn0w1" motorcycle covers that were sitting at ep 0 from
the old GN-at-0 convention. Both tables now agree with master_content_qubes
on the canonical convention (episode_number IS the display number; GN at -1).

Clients holding the previous v9/v7/v7 snapshot would keep rendering the stale
duplicate motorcycle slot 2 and the +1 title offset until they hard-refresh.
Bumping the cache key forces them to drop the old snapshot and refetch
against the now-correct DB on next load.

## Files Changed

| Change | File |
|--------|------|
| Modified | `.amplify-deploy` |
| Modified | `app/triad/components/codex/tabs/KnytTab.tsx` |

## Stats

 2 files changed, 4 insertions(+), 4 deletions(-)
