# Commit Brief: `fb9f56b` — restore canonical AGENTIQ_OS_CARTRIDGE + suppress pack-driven duplicate

| Field | Value |
|-------|-------|
| SHA | [`fb9f56b`](https://github.com/Kn0w-1/AigentZBeta/commit/fb9f56bd6c1776ffc39e48bf222189cfe2d769f5) |
| Author | Claude |
| Date | 2026-05-26T09:52:57Z |
| Branch | dev (direct push) |
| Type | `push` |
| Repo | Kn0w-1/AigentZBeta |

## Commit Message

```
restore canonical AGENTIQ_OS_CARTRIDGE + suppress pack-driven duplicate

The previous dedup attempt (b907029f) archived the wrong half of the
AgentiQ OS dual listing. It dropped AGENTIQ_OS_CARTRIDGE from
CODEX_DEFINITIONS — the hand-curated registration with the rich
tab structure (Home/Docs/Build/Bind/Deploy/Missions/Community) and
interactive React components (BoundedDelegationTab, DevPersonaTab,
RefStudioTab, RefRuntimeTab, DevMissionBoardTab). The pack-driven
duplicate (id `agentiq-os-codex`, auto-generated by packRegistry from
codexes/packs/agentiq-os/) was left visible — a simple markdown
viewer that doesn't match the slug ('agentiq-os-cartridge') that
metaMe's QuickLinksCard targets.

Two surgical edits restore canonical behaviour:

1) data/codex-configs.ts
   - AGENTIQ_OS_CARTRIDGE back in CODEX_DEFINITIONS
   - Comment block above the entry explains why the previous archive
     was wrong and how dedup is now achieved (skip list in registry).

2) app/api/codex/registry/_lib/packRegistry.ts
   - Add "agentiq-os" to the skip list alongside "agentiq" /
     "aigentiq" / "aigency". Pack content under
     codexes/packs/agentiq-os/items/*.md is still consumed via
     AgentiqCartridgeTab props inside the hand-curated cartridge's
     tabs — pack docs are not lost, only the duplicate auto-
     registration is.

Net effect:
  - Viewer shows ONE "AgentiQ OS" entry — the hand-curated cartridge
    with rich interactive tabs.
  - metaMe Cartridge's QuickLinksCard link to
    slug:'agentiq-os-cartridge' resolves correctly again.
  - Inline `agentiqos` tabGroup inside metaMe (via aiqOsTabsByGroup)
    continues to work — it reads AGENTIQ_OS_CARTRIDGE.tabs directly
    from the exported constant, independent of the registry.

https://claude.ai/code/session_01Ths4F8mcdYjDcKnjxnMy9n
```

## Body

The previous dedup attempt (b907029f) archived the wrong half of the
AgentiQ OS dual listing. It dropped AGENTIQ_OS_CARTRIDGE from
CODEX_DEFINITIONS — the hand-curated registration with the rich
tab structure (Home/Docs/Build/Bind/Deploy/Missions/Community) and
interactive React components (BoundedDelegationTab, DevPersonaTab,
RefStudioTab, RefRuntimeTab, DevMissionBoardTab). The pack-driven
duplicate (id `agentiq-os-codex`, auto-generated by packRegistry from
codexes/packs/agentiq-os/) was left visible — a simple markdown
viewer that doesn't match the slug ('agentiq-os-cartridge') that
metaMe's QuickLinksCard targets.

Two surgical edits restore canonical behaviour:

1) data/codex-configs.ts
   - AGENTIQ_OS_CARTRIDGE back in CODEX_DEFINITIONS
   - Comment block above the entry explains why the previous archive
     was wrong and how dedup is now achieved (skip list in registry).

2) app/api/codex/registry/_lib/packRegistry.ts
   - Add "agentiq-os" to the skip list alongside "agentiq" /
     "aigentiq" / "aigency". Pack content under
     codexes/packs/agentiq-os/items/*.md is still consumed via
     AgentiqCartridgeTab props inside the hand-curated cartridge's
     tabs — pack docs are not lost, only the duplicate auto-
     registration is.

Net effect:
  - Viewer shows ONE "AgentiQ OS" entry — the hand-curated cartridge
    with rich interactive tabs.
  - metaMe Cartridge's QuickLinksCard link to
    slug:'agentiq-os-cartridge' resolves correctly again.
  - Inline `agentiqos` tabGroup inside metaMe (via aiqOsTabsByGroup)
    continues to work — it reads AGENTIQ_OS_CARTRIDGE.tabs directly
    from the exported constant, independent of the registry.

https://claude.ai/code/session_01Ths4F8mcdYjDcKnjxnMy9n

## Files Changed

| Change | File |
|--------|------|
| Modified | `.amplify-deploy` |
| Modified | `app/api/codex/registry/_lib/packRegistry.ts` |
| Modified | `data/codex-configs.ts` |

## Stats

 3 files changed, 18 insertions(+), 9 deletions(-)
